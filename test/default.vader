Before:
  let b:ranger = yrange#ranger#default()
Given :
  Nothing
  :range1:
  .range1.
  :range2:
  .range2.
Execute (find next range):
  AssertEqual {'start':2, 'name':'range1'} ,b:ranger.search_start('c')
  AssertEqual {'start':4, 'name':'range2'} ,b:ranger.search_start('')
  AssertEqual 5,b:ranger.search_end('')

Given (find current range if cursor not at the beginning):
  :range:
  .range.
Do:
  $
Execute:
  AssertEqual {'start':1, 'name':'range'}, b:ranger.search_start('c')

