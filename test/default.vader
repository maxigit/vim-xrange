Given (a buffer with some commands):
  First line, not a range      
  !!: echo "a vim command"

  A multiline range
  !!named={
  var1=value1
  var2=ignoreme !! var2=value2 !!# var=ignoreme
  var3=value3!!}
  ^^^^ The end od the range

  !!! echo "a shell command"
Execute (find start and end):
  AssertEqual yrange#search#SearchNextCommandLine(), 2
  2
  AssertEqual yrange#search#SearchEndOfCurrentCommandLine(), 0
Execute (find multiline):
  2
  AssertEqual yrange#search#SearchNextCommandLine(), 5
  5
  AssertEqual yrange#search#SearchEndOfCurrentCommandLine(), 8
Execute (find across a multiline):
  5
  AssertEqual yrange#search#SearchNextCommandLine(), 11
  11
  AssertEqual yrange#search#SearchEndOfCurrentCommandLine(), 0
  
  
